import { countReset } from "console";
import { Request, Router, Response } from "express";
import {} from "express";

let route = Router();

route.route("/aeroports").get(async (req: Request, res: Response) => {});

export default route;
